apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cv-api.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cv-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}